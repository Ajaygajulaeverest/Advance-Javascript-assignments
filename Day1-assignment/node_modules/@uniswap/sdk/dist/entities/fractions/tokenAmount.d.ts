import { CurrencyAmount } from '@uniswap/sdk/dist/entities/fractions/currencyAmount';
import { Token } from '@uniswap/sdk/dist/entities/token';
import { BigintIsh } from '@uniswap/sdk/dist/constants';
export declare class TokenAmount extends CurrencyAmount {
    readonly token: Token;
    constructor(token: Token, amount: BigintIsh);
    add(other: TokenAmount): TokenAmount;
    subtract(other: TokenAmount): TokenAmount;
}
